spring:
  application:
    name: TallerAPI

  datasource:
    url: jdbc:mysql://localhost:3306/taller?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Felizcumple25
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: ${JWT_SECRET}

jwt:
  secret: ${JWT_SECRET:bXlzZWNyZXRrZXlzZWNyZXRrZXlzZWNyZXRrZXlzZWNyZXRrZXlzZWNyZXRrZXlzZWNyZXRr}
  expiration: ${JWT_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:604800000}